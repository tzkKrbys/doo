<%= form_for(@empathy) do |f| %>
  <% if @empathy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@empathy.errors.count, "error") %> prohibited this empathy from being saved:</h2>

      <ul>
      <% @empathy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :interest_id %><br>
    <%= f.number_field :interest_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
