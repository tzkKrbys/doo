<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this interest from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_name %><br>
    <%= f.text_field :user_name %>
  </div>
  <div class="field">
    <%= f.label :birthday %><br>
    <%= f.date_field :birthday, value: (f.object.birthday || Time.now.strftime("%Y-%m-%d")) %>
  </div>

  <%= f.hidden_field :id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
